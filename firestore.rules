rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /othello_games/{gameId} {
      // Any authenticated user (including anonymous) can read and write games
      allow read, write: if request.auth != null;
    }
  }
}
